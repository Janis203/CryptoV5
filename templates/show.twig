<!DOCTYPE html>
<html lang="en">
<head>
    <title> Crypto </title>
</head>
<body>
{% if data %}
    <h1>{{ data.name }} ({{ data.symbol }})</h1>
    <p>Rank: {{ data.rank }}</p>
    <p>Price: ${{ data.price }}</p>
{% else %}
    <p>Crypto not found</p>
{% endif %}
</body>
</html>